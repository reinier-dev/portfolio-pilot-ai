# OpenAI API Key - Get yours at https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Supabase Configuration - Get these from your Supabase project settings
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Security Configuration
# NODE_ENV - Ambiente de ejecución (development, production, test)
NODE_ENV=development

# ALLOWED_ORIGINS - Orígenes permitidos para CORS (separados por comas)
# En desarrollo: http://localhost:8080
# En producción: https://tu-dominio.vercel.app,https://www.tu-dominio.com
ALLOWED_ORIGINS=http://localhost:8080

# API_KEYS - Claves de API para proteger endpoints sensibles (separadas por comas)
# Genera claves seguras únicas. Ejemplo: openssl rand -hex 32
# IMPORTANTE: En producción, DEBES configurar al menos una API key
# Si no se configuran API keys, los endpoints quedarán SIN PROTECCIÓN (solo para desarrollo)
API_KEYS=
