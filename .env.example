# OpenAI API Key - Get yours at https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Supabase Server Configuration - Get these from your Supabase project settings
# These are used on the backend/server
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Client Configuration (Frontend)
# These variables are prefixed with VITE_ to be accessible in the client
# NOTA: La ANON_KEY es pública y segura para usar en el frontend
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Security Configuration
# NODE_ENV - Ambiente de ejecución (development, production, test)
NODE_ENV=development

# ALLOWED_ORIGINS - Orígenes permitidos para CORS (separados por comas)
# En desarrollo: http://localhost:8080
# En producción: https://tu-dominio.vercel.app,https://www.tu-dominio.com
ALLOWED_ORIGINS=http://localhost:8080

# API_KEYS - Claves de API para proteger endpoints sensibles (separadas por comas)
# NOTA: Ya NO se necesitan API keys porque usamos autenticación de usuarios
# Estas keys ahora son opcionales y solo para uso administrativo/interno
API_KEYS=
